<!DOCTYPE html>
<html>
<head>
  <title>Eat da Burger</title>

<!-- Bootstrap CDN -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

   <!-- Jquery -->
    <script src="https://code.jquery.com/jquery-3.1.1.js" integrity="sha256-16cdPddA6VdVInumRGo6IbivbERE8p7CQR3HzTBuELA=" crossorigin="anonymous"></script>

    <!-- my styles -->
    <link rel="stylesheet" type="text/css" href="/assets/css/burger_style.css">
</head>

<body>
<!--  <img src="../public/assets/img/burger.png"/>  -->
<div class="jumbotron text-center main-header">
      <h1>Eat da Burger</h1>
      <img src="/assets/img/burger.png">
        <h2>Enter your favorite Burger and hit Submit</h2>

</div>

<div class="container">
  <div class="row main-body text-center">
  <form method="POST" action="/" class="button-size">
    <div class="col-xs-9">
       <textarea type="text" cols = "80" rows="2" name="burger_name" placeholder="Enter your choice of burger name"></textarea>
    </div>
    <div class="col-xs-3 button">
    <button type="submit" class="btn btn-lg">Order Your Burger</button>
    </div>
  </form>
  </div><!-- row -->
</div>

<!-- //display all burger  -->
<div>
{{#each burgers}}
{{#unless this.devoured}}
<div>
  <div class="col-xs-2 col-md-2">{{this.id}}. {{this.burger_name}}</div>
</div>
    <div class="col-xs-2 col-md-2">
      <form action ="/update/{{id}}/?_method=PUT" method = "POST">

        <button type = "Submit"> Devour It!! </button>
        <input type="hidden" name="devoured" value="true">
      </form>
    </div>
    <br>
  {{/unless}}
      {{#if this.devoured}}
          <div class="col-xs-4 col-xs-offset-8">
          <div class="devoured text-muted"> {{this.id}}. {{this.burger_name}}
          </div>
          </div>
      {{/if}}
{{/each}}
<div>
</body>
</html>
